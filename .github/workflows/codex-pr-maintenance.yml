name: codex-pr-maintenance

on:
  pull_request:

permissions:
  actions: read
  contents: read

jobs:
  diagnose-pr-731:
    runs-on: ubuntu-latest
    steps:
      - name: Download latest resolver log
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail --location \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/actions/jobs/82556728070/logs" \
            --output resolver.log
          grep -n -E -C 30 '(Traceback|RuntimeError|SyntaxError|IndentationError|AssertionError|ValueError|error:|FAILED:|CONFLICT|unresolved|cannot|did not match|not found|not unique|Process completed with exit code)' \
            resolver.log > failure-summary.txt || true
          tail -n 350 resolver.log > resolver-tail.txt
          test -s failure-summary.txt
      - name: Upload diagnostic summary
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: pr-731-resolver-diagnostic-2
          path: |
            failure-summary.txt
            resolver-tail.txt
          retention-days: 1
