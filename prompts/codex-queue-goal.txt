/goal Act as the Fall of Nouraajd queue controller. Read and execute `prompts/codex-queue-controller.md` and all applicable `AGENTS.md` files as the authoritative workflow. Explicitly spawn worker subagents for eligible issues from `planning/fall_of_nouraajd_issue_proposals.xlsx`, keep at least eight implementation issues active whenever eight safe eligible non-conflicting issues exist, generate a unique controller ID for owner prefixes so concurrent controllers do not collide, keep a read-only project manager role when capacity permits to prepare prioritization briefs without overriding queue rules, use `python3 scripts/issue_queue.py shortlist --seed "$CONTROLLER_ID-<utc-cycle-id>" --include-rejected --json` as read-only issue-selection and active-capacity evidence before final review and `claim --issue`, rely on `activeClaims.unexpired` rather than raw `activeCount` for live-worker floor decisions, and keep one read-only QA role when capacity permits to review selection risk, tests, CI evidence, and merge readiness. For each issue, first publish a workbook-only claim PR that marks it `IN_PROGRESS`; after confirming the diff is XLSX-only and queue validation passed, merge workbook-only claim, terminal-status, heartbeat, reclaim, priority, dependency, or other approved queue-state PRs immediately with squash merge without waiting for GitHub Actions. Then create a dedicated Git worktree and implementation branch from the updated `origin/main`, delegate the verified issue to one worker, supervise root-cause analysis, minimal implementation, tests, review, commit, push, PR creation, and focused local checks plus CI-polled heavy Linux validation. Do not run local native builds, `ctest`, full Python suites, or coverage unless necessary; poll the PR `linux` job to success before squash auto-merge when CI polling supplies full validation evidence. Passing `build / linux` is enough when it covers the required checks, and if a PR is already merged stop waiting on its Actions run and continue from refreshed `origin/main`. Implementation branches must never modify the workbook; controller instances are its only writers. After the implementation PR actually merges, publish and merge a fresh workbook-only status PR marking the issue `DONE` with its summary and validation. Publish `BLOCKED`, `FAILED`, or `CANCELLED` through the same status-only process when appropriate. Serialize all XLSX updates, fetch and inspect `origin/main` when no new claims were made before declaring work exhausted, never bypass dependencies or failing checks, never destroy unreviewed user changes, and continue until no eligible safe task remains or the user stops the run.
