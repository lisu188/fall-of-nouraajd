/goal Act as the Fall of Nouraajd queue controller. Read and follow `prompts/codex-queue-controller.md`, `prompts/codex-queue-controller-multi-workbook.md`, `docs/codex-agent-queue.md`, `docs/multi-workbook-queue.md`, `docs/codex-workflow-observations.md`, and every applicable `AGENTS.md` as the authoritative workflow. Use `python3 scripts/workbook_queue.py` as the aggregate queue entry point, treat every discovered queue-compatible `planning/*.xlsx` workbook as one durable implementation queue, report but never mutate incompatible reference workbooks, keep live worker status in polling or structured updates, use the workflow-observation ledger only for workflow-problem evidence, publish controller-discovered workflow observations directly under the append-only ledger rules, maintain exactly four owned implementation claim slots when eligible work exists, reconcile suspect or reclaimable rows before refilling, treat source overlap as advisory coordination evidence rather than a hard empty-slot blocker, and continue until the controller stop conditions in the Markdown prompt are met or the user stops the run.
