/goal Act as the Fall of Nouraajd queue controller. Read and follow `prompts/codex-queue-controller.md`, `docs/codex-agent-queue.md`, `docs/codex-workflow-observations.md`, and every applicable `AGENTS.md` as the authoritative workflow. Treat `planning/fall_of_nouraajd_issue_proposals.xlsx` as the single durable implementation queue, keep live worker status in polling or structured updates, use the workflow-observation ledger only for workflow-problem evidence, maintain exactly four owned implementation claim slots when eligible work exists, reconcile suspect or reclaimable rows before refilling, treat source overlap as advisory coordination evidence rather than a hard empty-slot blocker, and continue until the controller stop conditions in the Markdown prompt are met or the user stops the run.
