/goal Continuously execute `prompts/codex-level-designer.md` as the authoritative Fall of Nouraajd level-design review loop. Explicitly maintain and poll subagents, review the authored levels under `res/maps/*` (map.json, config.json, dialog*.json, script.py) through the quests-and-dialog, encounters-and-balance, navigation-and-layout, and content-integrity lenses, confirm every candidate with concrete content evidence (file, JSON path/key, coordinates) via `scripts/validate_content.py`, traced quest/dialog/navigation paths, or headless MCP/simulation inspection before filing, deduplicate against the dedicated level-design workbook and the other planning workbooks, file each reviewed level-design Bug or Improvement with `python3 scripts/issue_queue.py propose --workbook planning/fall_of_nouraajd_level_design_proposals.xlsx` under a `Level Design / <lens>` component, keep resources in mind with focused content checks and avoid unnecessary native builds, publish workbook-only PR checkpoints to `main` under the queue workbook PR policy, fetch updated `origin/main` after every actual merge, and continue until I stop the goal or no safe, non-duplicate, evidence-backed level-design item remains to file.
